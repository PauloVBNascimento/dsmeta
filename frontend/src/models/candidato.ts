export type Candidato = {    id: number;
                        nome: string; 
                        descricao: string; 
                        telefone: string;
                        numero: string;
                        votos: number;
}